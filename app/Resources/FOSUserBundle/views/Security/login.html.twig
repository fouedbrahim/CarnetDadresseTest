{% extends "FOSUserBundle::layout.html.twig" %}
{% block title %}Connexion{% endblock %}
{% block fos_user_content %}
{% if error %}
    <div>{{ error|trans({}, 'FOSUserBundle') }}</div>
{% endif %}


    <div class="login-body">
        <ul id="top-bar" class="nav nav-tabs nav-justified">
                  
                    <li><a href="#login-access">Connexion</a></li>
                    
                </ul>
        <article class="container-login center-block">
            <section>
                
                <div class="tab-content tabs-login col-lg-12 col-md-12 col-sm-12 cols-xs-12">
                    <div id="login-access" class="tab-pane fade active in">
                       
                        <form action="{{ path("fos_user_security_check") }}" method="post" accept-charset="utf-8" autocomplete="off" role="form" class="form-horizontal">
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                            <div class="form-group ">
                                <label class="sr-only" for="username">{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}</label>
                                <input type="text" id="username" name="_username" placeholder="Username ou email" tabindex="1" value="{{ last_username }}" class="form-control" required="required" />
                            </div>
                            <div class="form-group ">
                                <label for="password" class="sr-only">{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}</label>
                                <input type="password" class="form-control" id="password" name="_password" tabindex="2" placeholder="Mot de Passe" value="" required="required" />
                            </div>
                            <div class="checkbox">
                                <label class="control-label" for="remember_me">
                                    <input type="checkbox" name="_remember_me" id="remember_me" value="on" class="" tabindex="3" /> {{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}
                                </label>
                            </div>
                            <br/>
                            <div class="form-group ">
                                <button type="submit" id="_submit" name="_submit" tabindex="5" class="btn btn-lg btn-primary" >
                                    {{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}
                                </button>
                                
                                <center>
                                 <li><a href="{{ path('fos_user_registration_register') }}" class="btn btn-primary">Inscription</a></li>
                                </center>
                            </div>
                        </form>
                    </div>
                </div>
            </section>
        </article>
    </div>
                                 <img src="{{ asset('public/img/car.png') }}">
{% endblock fos_user_content %}
